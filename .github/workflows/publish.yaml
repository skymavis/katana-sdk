name: Publish package
on:
  release:
    types: [created]
    branches: [main]

jobs:
  publish:
    if: ${{ true == startsWith(github.event.release.tag_name, 'swap-v3') }}
    runs-on: ubuntu-latest
    environment: npm-production

    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-node@v2
        with:
          node-version: 18
          registry-url: https://registry.npmjs.org/

      - name: Setup npmrc
        run: |
          echo "@sky-mavis:registry=https://registry.npmjs.org/" >> .npmrc
          echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_PUBLISH_TOKEN }}" >> .npmrc

      - name: Install dependencies
        run: cd packages/swap-v3 && yarn

      - name: Build
        run: cd packages/swap-v3 && yarn build

      - name: Publish @sky-mavis/katana-sdk/swap-v3
        run: cd packages/swap-v3 && npm publish --access=public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_PUBLISH_TOKEN }}